/target
/work
/out

# The Il2CppDumper release is fetched by tools/fetch-il2cppdumper.ps1 and verified
# against tools.lock.json. It is deliberately NOT vendored: a 12 MB binary in git
# is a blob nobody reviews, and the pinned digest gives us the same guarantee.
/tools/il2cppdumper/
