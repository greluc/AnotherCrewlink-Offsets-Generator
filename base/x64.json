{
  "_comment": [
    "Values for 64-bit Among Us builds (Epic Games, Microsoft Store) that cannot",
    "be derived from a dump. See base/x86.json for the reasoning behind each",
    "entry; only the numbers differ.",
    "",
    "These are carried from the hand-written V17.4.0 x64 offsets and have not",
    "been re-verified against a 64-bit build, because the machine this was",
    "written on only has the 32-bit Steam release installed. Run",
    "`acl-offsetgen verify` against an Epic or Store install before trusting a",
    "generated x64 file."
  ],

  "nativeRigidbodyPositionX": 176,

  "_serverManagerTail": [
    "static_fields -> singleton -> ServerManager.<CurrentRegion> -> its <Name>.",
    "",
    "Read out of the running game on 2026-08-24, because the carried value was",
    "wrong and so was upstream's. Both started at static_fields + 8, which is",
    "null in this build: the ServerManager instance sits at + 16. Walked live,",
    "[16, 40, 16] yields \"Europe\" while [8, 16, 8] and upstream's [8, 40, 16]",
    "both yield an empty string. The instance at + 16 is the coherent one --",
    "its CurrentUdpServer is matchmaker-eu.among.us, matching that region.",
    "",
    "40 is ServerManager.<CurrentRegion> and 16 is StaticRegionInfo.<Name>,",
    "both straight from the x64 dump. Only the first hop needed the game."
  ],
  "serverManagerTail": [16, 40, 16],

  "playerControlGameOptionsTail": [4],

  "functionPlaceholders": {
    "connectFunc": 4095,
    "showModStampFunc": 4095,
    "modLateUpdateFunc": 255,
    "fixedUpdateFunc": 4095,
    "pingMessageString": 4095
  },

  "_carriedSignatures": [
    "Empty on 64-bit: GameReader only scans for the write-path signatures inside",
    "an `if (!is_64bit)` branch, so they are never read here."
  ],
  "carriedSignatures": {},

  "flags": {
    "oldMeetingHud": false,
    "disableWriting": false,
    "newGameOptions": true
  }
}
